# Contributing

This repository is a competition prototype. Changes should keep the demo reproducible and preserve the distinction between synthetic evidence and real factory data.

Before opening a pull request:

```bash
make all
```

Do not commit credentials, real VINs, employee data, factory documents, or unlicensed proprietary datasets. New risk rules require a test case and a short explanation in `docs/method.md`.
